#include <stdio.h>
int main() {
    int a=15;
    if(a>=10&&a<=20)
    {
        printf("the number is within range");
    }
    else 
    {
        printf("not in range ");
    }
    return 0;
}